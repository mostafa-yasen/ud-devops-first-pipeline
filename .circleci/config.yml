version: 2.1

jobs:
  say-hello:
    docker:
      - image: alpine:latest
    steps:
      - run: echo 'Hello, '

  say-world:
    docker:
      - image: alpine:latest
    
    steps:
      - run: echo 'world!'

workflows:
  welcome:
    jobs:
      - say-hello
      - say_world:
        requires:
          - say_hello
